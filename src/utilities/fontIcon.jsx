import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
const FontICon = (className, icon) => {
  return <FontAwesomeIcon className={className} icon={icon} />;
};

export default FontICon;
